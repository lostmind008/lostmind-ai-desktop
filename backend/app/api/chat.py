"""
Chat API endpoints for LostMindAI Backend Service.

Handles chat sessions, message processing, and real-time communication
for both desktop and web clients.
"""

from fastapi import APIRouter

router = APIRouter()

# TODO: Implement chat endpoints in T18
# - POST /chat/sessions (create new chat session)
# - GET /chat/sessions/{session_id} (get chat history)
# - POST /chat/sessions/{session_id}/messages (send message)
# - DELETE /chat/sessions/{session_id} (delete session)